# 📂 Полный список файлов репозитория

## 📋 Основные файлы приложения

### `app.py` (950 строк)
**Главное приложение Streamlit**
- Точка входа приложения
- Экран аутентификации
- Главная панель (Dashboard)
- Базовая статистика
- Интеграция с RBAC

### `requirements.txt`
**Список зависимостей Python**
- streamlit==1.28.1
- sqlalchemy==2.0.23
- pandas==2.1.4
- plotly==5.18.0
- bcrypt==4.1.1
- psycopg2-binary==2.9.9
- и другие...

---

## 📁 Папка: `database/`
**Работа с базой данных (SQLAlchemy ORM)**

### `database/__init__.py` (пусто)
Маркер Python пакета

### `database/models.py` (380 строк)
**SQLAlchemy модели**

Таблицы:
- `Athlete` — профиль спортсмена (15 полей)
- `CompetitionResult` — результаты соревнований (11 полей)
- `MedicalData` — мед-биолог показатели (30+ полей)
- `DevelopmentPlan` — планы развития (9 полей)
- `User` — пользователи системы (7 полей)
- `AuditLog` — логирование действий (7 полей)

### `database/connection.py` (45 строк)
**Подключение к БД**
- SQLite (локально)
- PostgreSQL (production)
- Session factory
- Engine кеширование

### `database/db_init.py` (25 строк)
**Инициализация БД**
- Создание таблиц
- Миграции

---

## 📁 Папка: `utils/`
**Утилиты приложения**

### `utils/__init__.py` (пусто)
Маркер Python пакета

### `utils/auth.py` (100 строк)
**Система аутентификации**

Функции:
- `authenticate_user(email, password)` — проверка учётных данных
- `init_session_state()` — инициализация сессии
- Декораторы: `@require_auth()`, `@require_role()`

Тестовые пользователи (hardcoded для demo):
- admin@ocr.ru (admin123)
- curator_athletics@ocr.ru (curator123)
- athlete@example.com (athlete123)

### `utils/rbac.py` (140 строк)
**Управление доступом по ролям (RBAC)**

Матрица доступа:
- **admin** — полный доступ
- **curator** — доступ к своему виду спорта
- **athlete** — только свой профиль (read-only)

Функции:
- `check_access(permission)` — проверка разрешения
- `get_user_sports(user_id)` — виды спорта пользователя
- `can_view_athlete()`, `can_edit_athlete()`, `can_delete_data()`
- `@require_permission()` — декоратор

### `utils/charts.py` (380 строк)
**Построение интерактивных графиков (Plotly)**

Графики:
- `plot_competition_results_trend()` — динамика результатов
- `plot_vo2max_trend()` — МПК за 2 года
- `plot_heart_rate_zones()` — пульсовые зоны
- `plot_morphometry_trend()` — морфометрия (вес, % жира)
- `plot_blood_markers()` — показатели крови

### `utils/validators.py` (130 строк)
**Валидация входных данных**

Функции:
- `validate_email()` — email формат
- `validate_phone()` — телефон формат
- `validate_date()` — дата (не в будущем)
- `validate_athlete_data()` — профиль спортсмена
- `validate_medical_data()` — мед. показатели
- `validate_competition_result()` — результат соревнования

---

## 📁 Папка: `scripts/`
**Вспомогательные скрипты**

### `scripts/__init__.py` (пусто)
Маркер Python пакета

### `scripts/init_db.py` (95 строк)
**Инициализация БД с мок-данными**

Функция:
- `populate_database()` — заполнение БД

Что делает:
1. Инициализирует таблицы
2. Удаляет старые данные (если есть)
3. Генерирует 30 спортсменов
4. Генерирует 450 результатов соревнований
5. Генерирует 300 мед. записей
6. Выводит статистику

Запуск:
```bash
python scripts/init_db.py
```

### `scripts/generate_mock_data.py` (450 строк)
**Генератор реалистичных мок-данных**

Русские имена:
- Мужские: 18 имён (Алексей, Артём, Вадим...)
- Женские: 13 имён (София, Ева, Анна...)
- Фамилии: 19 вариантов для каждого пола

Данные:
- 10 видов спорта (Лёгкая атлетика, Плавание...)
- 19 регионов РФ
- Реалистичные мед. показатели

Функции:
- `generate_athlete_name(gender)` — ФИО
- `generate_athletes(count)` — список спортсменов
- `generate_competition_results(athlete_id)` — 15 результатов
- `generate_medical_data(athlete_id)` — 10 мед. записей

---

## 📁 Папка: `.streamlit/`
**Конфигурация Streamlit**

### `.streamlit/config.toml`
**Конфиг приложения**
- Тема оформления (цвета, шрифты)
- Порт (8501)
- Логирование

### `.streamlit/secrets.toml.example`
**Пример секретов (для git)**
- SQLite (по умолчанию)
- PostgreSQL (production)
- Streamlit Cloud настройки

⚠️ **Реальный `secrets.toml` не коммитить!**

---

## 📁 Папка: `data/`
**Локальные данные**

### `data/olympic_reserve.db`
**SQLite база данных**
- Генерируется автоматически при `python scripts/init_db.py`
- ~500 КБ с 30 спортсменами
- Исключена из git (`.gitignore`)

---

## 📄 Документация (Markdown файлы)

### `README.md` (200 строк)
**Главная документация проекта**
- Описание системы
- Компоненты
- Быстрый старт
- Структура проекта
- Тестовые учётные данные
- Развёртывание на Streamlit Cloud
- Безопасность
- Лицензия

### `QUICKSTART.md` (80 строк)
**Быстрый старт за 5 минут**
- 5 шагов к запуску
- Тестовые учётные данные
- Проверка функционала
- Решение частых проблем

### `RUNNING_GUIDE.md` (350 строк)
**Полное руководство по запуску**

Разделы:
1. **Локальный запуск** — детальные инструкции
2. **Streamlit Cloud** — step-by-step деплой
3. **Структура проекта** — описание папок
4. **Тестовые данные** — какие данные генерируются
5. **Системные требования** — Python версия, пакеты
6. **PostgreSQL** — использование в production
7. **Решение проблем** — FAQ

### `ARCHITECTURE.md` (400 строк)
**Архитектура и внутреннее устройство**

Разделы:
1. **Архитектура** — диаграммы
2. **Схема БД** — структура таблиц
3. **RBAC система** — уровни доступа
4. **Компоненты визуализации** — графики
5. **Процесс добавления спортсмена** — workflow
6. **Технический стек** — версии зависимостей
7. **Развёртывание** — Docker, K8s
8. **Безопасность** — имплементированное и рекомендуемое
9. **Масштабируемость** — рекомендации

### `DEPLOYMENT.md` (опционально)
**Дополнительный гайд по развёртыванию**
- Docker инструкции
- Kubernetes манифесты
- CI/CD pipeline
- Мониторинг и логирование

---

## 📄 Системные файлы

### `.gitignore` (30 строк)
**Исключения из Git**
- `__pycache__/` — Python кеш
- `venv/` — виртуальное окружение
- `*.db` — БД файлы
- `.env` — переменные окружения
- `.streamlit/secrets.toml` — локальные секреты
- `.DS_Store`, `Thumbs.db` — OS файлы

### `.env.example` (опционально)
**Пример переменных окружения**
```
DATABASE_URL=sqlite:///./data/olympic_reserve.db
DEBUG=true
SECRET_KEY=your-secret-key
```

---

## 📊 Статистика проекта

| Метрика | Значение |
|---------|----------|
| **Всего файлов** | 30+ |
| **Python файлов** | 15 |
| **Markdown файлов** | 4 |
| **Строк Python кода** | ~3500 |
| **Строк документации** | ~1500 |
| **Размер репозитория** | ~2 МБ (без БД) |
| **Зависимостей** | 12 пакетов |

---

## 🔍 Навигация по кодовой базе

### Для новичков
1. Начни с `QUICKSTART.md`
2. Прочитай `README.md`
3. Посмотри `app.py` (главное приложение)
4. Изучи `database/models.py` (структура БД)

### Для разработчиков
1. Изучи `ARCHITECTURE.md`
2. Посмотри `database/` (модели БД)
3. Посмотри `utils/` (бизнес-логика)
4. Прочитай `scripts/generate_mock_data.py` (генерация данных)

### Для DevOps/SRE
1. Прочитай `RUNNING_GUIDE.md`
2. Посмотри `.streamlit/config.toml`
3. Изучи `requirements.txt`
4. Посмотри `ARCHITECTURE.md` (безопасность, масштабируемость)

### Для тестирования
1. Стартовые данные в `scripts/generate_mock_data.py`
2. Тестовые учётные данные в `utils/auth.py`
3. Валидаторы в `utils/validators.py`

---

## 🚀 Быстрые команды

```bash
# Клонирование
git clone https://github.com/username/olympic-reserve-registry.git

# Установка
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Инициализация БД
python scripts/init_db.py

# Запуск
streamlit run app.py

# На другом порту
streamlit run app.py --server.port 8502

# С debug логами
streamlit run app.py --logger.level=debug

# Для Streamlit Cloud
git push origin main
# (автоматический деплой)
```

---

**Всего файлов: 30+**  
**Готово к работе: ✅**  
**Лицензия: Закрытая (ОКР)**
