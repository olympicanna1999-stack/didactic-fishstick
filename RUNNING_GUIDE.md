# 🚀 Руководство по запуску "Цифровой реестр олимпийского резерва"

## 📋 Содержание
1. [Локальный запуск](#локальный-запуск)
2. [Запуск на Streamlit Cloud](#запуск-на-streamlit-cloud)
3. [Структура проекта](#структура-проекта)
4. [Тестовые данные](#тестовые-данные)
5. [Системные требования](#системные-требования)
6. [Решение проблем](#решение-проблем)

---

## 🖥️ Локальный запуск

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/your-username/olympic-reserve-registry.git
cd olympic-reserve-registry
```

### Шаг 2: Создание виртуального окружения

**На macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**На Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Инициализация БД с мок-данными
```bash
python scripts/init_db.py
```

Ожидайте вывода:
```
🔄 Инициализация базы данных...
✅ База данных инициализирована
📝 Генерирую мок-данные...
✅ Добавлено 30 спортсменов
✅ Добавлены результаты соревнований
✅ Добавлены медико-биологические показатели

📊 Итого:
   👥 Спортсменов: 30
   🏆 Результатов: 450
   🏥 Медицинских записей: 300

✅ База данных успешно заполнена!
```

### Шаг 5: Запуск приложения
```bash
streamlit run app.py
```

Приложение откроется в браузере: **http://localhost:8501**

---

## 🌐 Запуск на Streamlit Cloud

### Подготовка GitHub репозитория

1. **Создание репозитория на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Olympic Reserve Registry"
   git branch -M main
   git remote add origin https://github.com/your-username/olympic-reserve-registry.git
   git push -u origin main
   ```

2. **Убедитесь, что в репозитории есть:**
   - `app.py`
   - `requirements.txt`
   - `.gitignore` (исключает `data/*.db` и `venv/`)
   - Папки `database/`, `utils/`, `scripts/`

### Развёртывание на Streamlit Cloud

1. **Перейдите на [share.streamlit.io](https://share.streamlit.io)**

2. **Войдите через GitHub**
   - Нажмите "Sign in with GitHub"
   - Авторизуйтесь

3. **Создание нового приложения**
   - Нажмите "Create app"
   - Выберите:
     - Repository: `your-username/olympic-reserve-registry`
     - Branch: `main`
     - Main file path: `app.py`

4. **Настройка секретов (обязательно)**
   - В появившейся странице нажмите ⋮ → Settings
   - Перейдите на вкладку "Secrets"
   - Добавьте (если используете PostgreSQL):
     ```
     [connections.postgresql]
     dialect = "postgresql"
     host = "your-host.com"
     port = "5432"
     database = "olympic_reserve"
     username = "your_username"
     password = "your_password"
     ```
   - Или (для SQLite на Streamlit Cloud):
     ```
     DATABASE_URL = "sqlite:///./data/olympic_reserve.db"
     ```

5. **Перезагрузка приложения**
   - После сохранения секретов приложение автоматически перезагружается

---

## 📁 Структура проекта

```
olympic-reserve-registry/
├── 📄 app.py                      # Главное приложение (точка входа)
├── 📄 requirements.txt             # Зависимости Python
├── 📄 README.md                    # Основная документация
├── 📄 RUNNING_GUIDE.md             # Этот файл
├── 📄 .gitignore                   # Исключения для Git
│
├── 📁 database/
│   ├── __init__.py
│   ├── 📄 models.py                # SQLAlchemy модели (Athlete, CompetitionResult и т.д.)
│   ├── 📄 connection.py            # Подключение к БД (SQLite/PostgreSQL)
│   └── 📄 db_init.py               # Инициализация схемы
│
├── 📁 utils/
│   ├── __init__.py
│   ├── 📄 auth.py                  # Аутентификация и RBAC
│   ├── 📄 rbac.py                  # Управление доступом по ролям
│   ├── 📄 charts.py                # Построение графиков Plotly
│   └── 📄 validators.py            # Валидация входных данных
│
├── 📁 scripts/
│   ├── __init__.py
│   ├── 📄 init_db.py               # Скрипт инициализации БД
│   └── 📄 generate_mock_data.py    # Генератор мок-данных
│
├── 📁 .streamlit/
│   ├── 📄 config.toml              # Конфигурация Streamlit (тема, порт)
│   └── 📄 secrets.toml             # Локальные секреты (не коммитить!)
│
└── 📁 data/
    └── 📄 olympic_reserve.db       # SQLite БД (генерируется автоматически)
```

---

## 🧪 Тестовые данные

### Учётные данные пользователей

| Роль | Email | Пароль | Доступ |
|------|-------|--------|--------|
| **Администратор** | `admin@ocr.ru` | `admin123` | Все функции, все спортсмены |
| **Куратор (Лёгкая атлетика)** | `curator_athletics@ocr.ru` | `curator123` | Редактирование спортсменов в своём виде спорта |
| **Куратор (Плавание)** | `curator_swimming@ocr.ru` | `curator123` | Редактирование спортсменов в своём виде спорта |
| **Спортсмен** | `athlete@example.com` | `athlete123` | Просмотр своего профиля (read-only) |

### Мок-данные

**30 спортсменов** с реалистичными данными:
- ✅ Русские имена (18 мужских + 13 женских)
- ✅ Русские фамилии
- ✅ Различные виды спорта (Лёгкая атлетика, Плавание, Лыжи, Биатлон, Гребля и т.д.)
- ✅ Региональная привязка (РФ регионы)
- ✅ Дата рождения (15-22 года)

**450 результатов соревнований** (15 на спортсмена):
- Даты за последние 2 года
- Различные уровни (internacional, national, regional)
- Результаты и места
- Отметки о личных рекордах

**300 медико-биологических записей** (10 на спортсмена):
- ЧСС (в покое и максимальная)
- МПК (абсолютный и относительный)
- ПАНО
- Пульсовые зоны (1-5)
- Морфометрия (рост, вес, % жира, безжировая масса)
- Динамометрия (обе руки)
- Показатели крови (гемоглобин, гематокрит, лактат)
- Объём лёгких

---

## ⚙️ Системные требования

### Минимальные требования
- **Python:** 3.10 или выше
- **pip:** 21.0 или выше
- **ОС:** macOS, Linux, Windows

### Проверка версии Python
```bash
python --version
# или на Linux/macOS
python3 --version
```

### Требуемые пакеты
Все зависимости указаны в `requirements.txt`:
- `streamlit==1.28.1` — веб-фреймворк
- `sqlalchemy==2.0.23` — ORM
- `pandas==2.1.4` — обработка данных
- `plotly==5.18.0` — интерактивные графики
- `bcrypt==4.1.1` — хеширование паролей
- и другие...

### Объём памяти
- Минимум: 512 MB RAM
- Рекомендуется: 2 GB RAM

---

## 🔧 Использование PostgreSQL (Production)

По умолчанию приложение использует SQLite. Для production рекомендуется PostgreSQL.

### 1. Установка PostgreSQL
```bash
# macOS (через brew)
brew install postgresql

# Ubuntu/Debian
sudo apt-get install postgresql postgresql-contrib

# Windows
# Скачать с https://www.postgresql.org/download/windows/
```

### 2. Создание БД
```bash
createdb olympic_reserve
```

### 3. Переменная окружения
Создайте файл `.streamlit/secrets.toml`:
```toml
[connections.postgresql]
dialect = "postgresql"
host = "localhost"
port = "5432"
database = "olympic_reserve"
username = "postgres"
password = "your_password"
```

### 4. Инициализация
```bash
python scripts/init_db.py
```

---

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'streamlit'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate      # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "Database is locked"

**Причина:** SQLite заблокирована одновременным доступом

**Решение:**
1. Закройте все окна приложения
2. Удалите `data/olympic_reserve.db`
3. Переинициализируйте БД: `python scripts/init_db.py`

### Проблема: "Port 8501 is already in use"

**Решение:**
```bash
# Запустите на другом порту
streamlit run app.py --server.port 8502
```

### Проблема: Данные не сохраняются

**Проверьте:**
1. Папка `data/` существует и доступна на запись
2. Нет ошибок в консоли
3. Используется корректный CONNECTION_STRING

### Проблема: На Streamlit Cloud приложение не запускается

**Проверьте:**
1. Все файлы в репозитории (кроме `venv/` и `data/*.db`)
2. `requirements.txt` актуальный
3. Секреты добавлены в Settings → Secrets
4. Нет ошибок Python в `app.py`

Смотрите логи в Streamlit Cloud: Manage app → Logs

---

## 📞 Поддержка и ошибки

###报告ошибок
Если вы обнаружили ошибку:
1. Убедитесь, что используете последнюю версию из Git
2. Проверьте логи: `streamlit run app.py --logger.level=debug`
3. Создайте Issue на GitHub с описанием

### Вопросы и предложения
- 📧 Email: support@your-domain.com
- 💬 Discussions: GitHub Discussions
- 🐛 Issues: GitHub Issues

---

## 📚 Дополнительные ресурсы

- [Streamlit Документация](https://docs.streamlit.io/)
- [SQLAlchemy ORM](https://docs.sqlalchemy.org/)
- [Plotly Графики](https://plotly.com/python/)
- [PostgreSQL Документация](https://www.postgresql.org/docs/)

---

**Версия:** 1.0  
**Последнее обновление:** Ноябрь 2025  
**Лицензия:** Закрытая (ОКР)
